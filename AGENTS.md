# Agent Instructions

The canonical instructions for AI agents working in this repository live
in [`CLAUDE.md`](./CLAUDE.md). Read that file.

This `AGENTS.md` exists only as a redirect for tools and agents that look
for the generic `AGENTS.md` convention. Do not edit instructions here —
edit `CLAUDE.md` and let this pointer stay short so it never drifts.
