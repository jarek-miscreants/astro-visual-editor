---
const variant = "primary";
const classes = "bg-blue-600 text-white";
---

<button class={classes} type="button">Click me</button>
<div class:list={["base", variant === "primary" && "bg-blue-600"]}>Listed</div>
